{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "taskName": "build",
            "command": "browserify -t [ babelify --presets [ react es2015 ] ] script/start.js -o bundle.js",
            "type": "shell"
        }
    ]
}